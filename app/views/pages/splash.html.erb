<% unless current_user %>
  <%= link_to('Login', new_user_session_path)  %>
<% end %>

<%= current_user.is_provider ? "Provider: #{current_user.first_name}" : "Patient: #{current_user.first_name}" %>
