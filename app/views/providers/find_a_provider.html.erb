<h1>Find a Provider</h1>

<table>
  <thead>
    <tr>
      <th>Provider</th>
      <th>Bio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @providers.each do |provider| %>
      <tr>
        <td><%= link_to "#{provider.first_name}", appointments_book_path(provider_id: provider.id) %></td>
        <td><%= provider.bio %></td>
      </tr>
    <% end %>
  </tbody>
</table>
